<p>
  Mentor:<br />
  <%= link @mentor.name, to: Routes.admin_mentor_path(@conn, :show, @mentor), class: "link" %>
</p>

<div class="h-4"></div>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_timesheet_entry_path(@conn, :update, @timesheet_entry)) %>

<div class="h-8"></div>
