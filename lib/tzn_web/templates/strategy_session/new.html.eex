<h1>New Strategy session</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.strategy_session_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.strategy_session_path(@conn, :index) %></span>
